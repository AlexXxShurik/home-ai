[tool.poetry]
name = "home-ai-backend"
version = "0.1.0"
description = "Home AI Backend - FastAPI + Tuya"
authors = []
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
fastapi = ">=0.100.0"
uvicorn = {version = ">=0.22.0", extras = ["standard"]}
tinytuya = ">=1.20.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

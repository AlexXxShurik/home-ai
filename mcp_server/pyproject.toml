[tool.poetry]
name = "home-ai-mcp-server"
version = "0.1.0"
description = "Home AI MCP Server"
authors = []
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
mcp = "<2"
httpx = ">=0.27.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
